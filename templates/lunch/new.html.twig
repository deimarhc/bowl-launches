{% extends 'base.html.twig' %}

{% block title %}{% trans %}New lunch{% endtrans %}{% endblock %}

{% block body %}
    <h1 class="w-100 h3 mb-3 font-weight-normal">{% trans %}Create new lunch{% endtrans %}</h1>

    <div class="w-50">
        {{ include('lunch/_form.html.twig') }}
    </div>

    <a class="w-100 mt-1 btn btn-dark" href="{{ path('lunch_index') }}">{% trans %}Back to list{% endtrans %}</a>
{% endblock %}
