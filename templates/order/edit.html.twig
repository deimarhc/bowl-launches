{% extends 'base.html.twig' %}

{% block title %}{% trans %}Edit Order{% endtrans %}{% endblock %}

{% block body %}
    <h1 class="w-100 h3 mb-3 font-weight-normal">{% trans %}Edit order{% endtrans %}</h1>

    <div class="w-50">
        {{ include('order/_form.html.twig', {'button_label': 'Update'}) }}
    </div>

    <div class="w-100 btn-group-lg mt-2">
        {{ include('order/_delete_form.html.twig') }}
        <a href="{{ path('order_index') }}" class="btn btn-dark">{% trans %}Back to list{% endtrans %}</a>
    </div>
{% endblock %}
